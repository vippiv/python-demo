import random
# print(random.randint(1, 3))  # random.randint(start, end)，生成包含指定数的随机数
# print(random.random())  # 生成0-1之间的随机数
# print(random.randrange(0, 6, 2))  # random.randrange(start, end, step) 生成指定范围内的随机数，包含start，不含end，step是步长
# print(random.uniform(0, 5.3))  # 随机生成包含start，end的浮点数
# print(random.choice((1, 5, 9, 7, 5, 6)))  # 从序列(元祖，列表，字符串)中随机选取一个元素。seq需要是一个序列，比如list、元组、字符串
# print(random.sample((1, 5, 9, 7, 5, 6), 3))  # 从指定序列(元祖，列表，字符串)中，随机获取指定个数元素，生成一个新序列，不改变原来序列
# random.shuffle([1, 5, 9, 7, 5, 6])  # 将序列（列表）的所有元素随机排序
